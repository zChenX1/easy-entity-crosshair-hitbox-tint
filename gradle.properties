# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties (1.21.11 was the last version published with yarn mappings;
# Minecraft 26.3 ships deobfuscated, so no mappings dependency is needed.)
minecraft_version=26.3
loader_version=0.19.5
loom_version=1.17.21

# Mod Properties
mod_version=1.0.0
maven_group=zchenx
archives_base_name=easy_entity_crosshair_hitbox_tint
java_version=25
# Optional integration (compile only, never a runtime dependency)
modmenu_version=21.0.0-beta.1
