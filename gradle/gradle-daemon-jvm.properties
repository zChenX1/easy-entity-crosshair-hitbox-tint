# Gradle daemon JVM criteria: Fabric Loom 1.17 requires a Java 21+ runtime to run,
# while this project targets Java 25. Gradle auto-detects the JDK (e.g. ~/.jdks) and
# starts the build with it, so `gradlew build` works without setting JAVA_HOME.
toolchainVersion=25
